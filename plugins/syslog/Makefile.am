
plugindir = \
    $(libdir)/guardian

plugin_LTLIBRARIES = \
    syslog.la

syslog_la_SOURCES = \
    syslog.c

syslog_la_LDFLAGS =   \
    -avoid-version       \
    -module              \
    $(PLATFORM_LDFLAGS)
