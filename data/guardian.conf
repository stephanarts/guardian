# guardian.conf
#
# This is the guardian system-wide configuration file.
# See guardian.conf(5) for more information.

# db_type:
db_type = sqlite3

db_path = /tmp/guardian.db

db_host =
db_host = 127.0.0.1

db_port = 

autoregister_host = true
